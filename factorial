// Factorial 

#include <iostream>
using namespace std;

int main()
{
    int num, fact = 1;

    cout << "\n\t Enter number: ";
    cin >> num;

    while(num > 0)
    {
        fact *= num;
        num = num - 1;
    }

    cout << "\n\t Factorial: " << fact;

    return 0;
}
